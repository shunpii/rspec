describe'四則演算'do
    context'足し算'do
        it'１＋１は２になる'do
            expect(1+1).to eq2
        end
    end
    context'足し算'do
        it'１＋１は２になる'do
            expect(1+1).to eq3
        end
    end
end